# セットアップ手順

このBootcampで使用する環境のセットアップ手順です。
所要時間は約10分です。

---

## Step 1: Databricks Free Edition の登録

### 1-1. 登録ページにアクセス

https://www.databricks.com/jp/learn/free-edition にブラウザからアクセスしてください。

### 1-2. メールアドレスを入力

ご自身のメールアドレスを入力し、「**メールアドレスで続行**」をクリック。

> ⚠️ **重要:** 過去にDatabricks Free Editionを利用したことがあるアカウントは、無料クレジットが枯渇している場合があります。その場合は別のメールアドレスで新規登録してください（Gmailの場合は `your.name+bootcamp@gmail.com` のようなエイリアスも使えます）。

### 1-3. 認証コードを入力

登録したメールアドレス宛に認証コードが届きます。そのコードを入力して「**続行**」をクリック。

### 1-4. 登録完了

自動的にワークスペースが作成されます。URLは `https://dbc-XXXXXXXX-XXXX.cloud.databricks.com/` の形式です。

---

## Step 2: 表示言語を日本語に変更

1. 画面右上のユーザーアイコンをクリック
2. 「**Settings**」を選択
3. 左メニュー「**Preferences**」→「**Language**」を「**日本語**」に変更

---

## Step 3: GitHubリポジトリのクローン

参加者のワークスペースにBootcamp用のノートブックを取り込みます。

### 3-1. Gitフォルダの作成

1. 左メニューから「**ワークスペース**」を開く
2. 自分のユーザー名のフォルダ（Users > your.email@...）を選択
3. 右上の「**作成**」→「**Gitフォルダ**」を選択

### 3-2. リポジトリURLを入力

以下の情報を入力して「**Gitフォルダを作成**」をクリック:

- **GitリポジトリのURL**: `https://github.com/databricks-solutions/databricks-japan-bootcamp`
- **Gitプロバイダー**: `GitHub`
- **Gitフォルダ名**: `databricks-japan-bootcamp`（自動入力されます）

※ 数秒で完了します

### 3-3. ノートブックを開く

クローンされたフォルダの中から、以下のパスのノートブックを開いてください:

```
databricks-japan-bootcamp/
└── databricks-data-ai-bootcamp/
    └── 20260427/
        └── notebooks/
            ├── 00_main.py              ← 後半ハンズオン①②で使用
            └── 01_ai_data_processing.py ← 後半ハンズオン③で使用
```

---

## Step 4: サンプルデータの準備（AIデータ加工ハンズオン用）

後半最後の「AIでのデータ加工」ハンズオンで、ECサイト「Brick EC」のサンプルデータを使用します。
**AIハンズオンが始まる前に**以下を実行してください（所要時間: 約2〜3分）。

1. `notebooks/setup_sample_data.py` を開く
2. 上部の「**すべてを実行**」をクリック
3. 実行完了を待つ

> 💡 このノートブックはサンプルテーブルを5つ作成します（`workspace.bootcamp_osaka.*`）
>
> - `gold_users`（500行）
> - `gold_products`（38行）
> - `gold_transactions`（3,000行）
> - **`gold_reviews`（1,000行）** ← AIハンズオンで使用
> - **`support_inquiries`（300行）** ← AIハンズオン発展問題で使用

> ⚠️ このステップを実行せずに AI ハンズオンに入るとテーブル not found エラーになります

---

## トラブルシューティング

### 「サーバーレスが見つからない」と表示される

ワークスペース作成直後は、サーバーレスコンピュートの初期化に時間がかかることがあります。
1〜2分待ってからページを再読み込みしてください。

### 「クレジットが枯渇しています」と表示される

過去に使用したアカウントの可能性があります。別のメールアドレスで新規登録してください。

### Gitフォルダが作成できない

Gitプロバイダーの認証を求められる場合があります。その場合は：
1. 「**パーソナルアクセストークンを追加**」は**スキップ**
2. パブリックリポジトリなのでトークン不要です

### その他

会場スタッフ（TA）にお声がけください。赤い名札をつけています。

---

## 準備完了！

以上でセットアップは完了です。
前半のAI/BIハンズオンは、講師の指示に従って進めてください。
